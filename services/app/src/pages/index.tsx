import React from 'react';
import { Button } from '@common/component';

const Home = () => {
  return (
    <div className="p-2">
      <Button>
        <span className="text-white">버튼</span>
      </Button>
    </div>
  );
};

export default Home;
